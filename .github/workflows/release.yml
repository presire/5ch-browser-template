name: Release (Linux)

on:
  push:
    tags:
      - "v*"

jobs:
  build-linux-x64:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            rpm \
            fcitx5-frontend-gtk3

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install desktop deps
        run: npm ci
        working-directory: apps/desktop

      - name: Build AppImage, deb, rpm
        working-directory: apps/desktop
        run: |
          TMP_CONFIG="$(mktemp /tmp/tauri-linux-build-override.XXXXXX.json)"
          cat > "$TMP_CONFIG" <<'JSON'
          {
            "bundle": {
              "icon": ["icons/icon.png"],
              "targets": ["appimage", "deb", "rpm"]
            }
          }
          JSON
          npm run tauri:build -- --config "$TMP_CONFIG"
          rm -f "$TMP_CONFIG"

      - name: Repack AppImage with bundled libs
        run: |
          sudo apt-get install -y libfuse2
          cd target/release/bundle/appimage
          APPIMAGE="$(ls *.AppImage)"
          chmod +x "$APPIMAGE"
          ./"$APPIMAGE" --appimage-extract

          # Bundle FreeType to avoid symbol mismatch with bundled WebKit2GTK
          cp /usr/lib/x86_64-linux-gnu/libfreetype.so.6* squashfs-root/usr/lib/

          # Bundle Fcitx5 client library (required by im-fcitx5.so for daemon communication)
          cp /usr/lib/x86_64-linux-gnu/libFcitx5GClient.so.2* squashfs-root/usr/lib/ 2>/dev/null || true

          # Bundle GTK3 IM modules for Fcitx5/IBus input method support
          IMMODULES_DIR=squashfs-root/usr/lib/x86_64-linux-gnu/gtk-3.0/3.0.0/immodules
          mkdir -p "$IMMODULES_DIR"
          cp /usr/lib/x86_64-linux-gnu/gtk-3.0/3.0.0/immodules/im-fcitx5.so "$IMMODULES_DIR/" 2>/dev/null || true
          cp /usr/lib/x86_64-linux-gnu/gtk-3.0/3.0.0/immodules/im-ibus.so "$IMMODULES_DIR/" 2>/dev/null || true

          # Regenerate immodules.cache from ALL bundled modules (do not overwrite on failure)
          CACHE_FILE=squashfs-root/usr/lib/x86_64-linux-gnu/gtk-3.0/3.0.0/immodules.cache
          gtk-query-immodules-3.0 "$IMMODULES_DIR"/*.so > "${CACHE_FILE}.new" 2>/dev/null && mv "${CACHE_FILE}.new" "$CACHE_FILE" || rm -f "${CACHE_FILE}.new"

          # Download appimagetool and repackage
          wget -q "https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-x86_64.AppImage" -O appimagetool
          chmod +x appimagetool
          ARCH=x86_64 ./appimagetool --no-appstream squashfs-root "$APPIMAGE"

      - name: Collect artifacts
        run: |
          mkdir -p out
          cp target/release/bundle/appimage/*.AppImage out/ember_amd64.AppImage
          cp target/release/bundle/deb/*.deb out/ember_amd64.deb
          cp target/release/bundle/rpm/*.rpm out/ember_amd64.rpm

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-x64
          path: out/*

  build-linux-aarch64:
    runs-on: ubuntu-22.04-arm
    steps:
      - uses: actions/checkout@v4

      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            rpm \
            fcitx5-frontend-gtk3

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install desktop deps
        run: npm ci
        working-directory: apps/desktop

      - name: Build AppImage, deb, rpm
        working-directory: apps/desktop
        run: |
          TMP_CONFIG="$(mktemp /tmp/tauri-linux-aarch64-build-override.XXXXXX.json)"
          cat > "$TMP_CONFIG" <<'JSON'
          {
            "bundle": {
              "icon": ["icons/icon.png"],
              "targets": ["appimage", "deb", "rpm"]
            }
          }
          JSON
          npm run tauri:build -- --config "$TMP_CONFIG"
          rm -f "$TMP_CONFIG"

      - name: Repack AppImage with bundled libs
        run: |
          sudo apt-get install -y libfuse2
          cd target/release/bundle/appimage
          APPIMAGE="$(ls *.AppImage)"
          chmod +x "$APPIMAGE"
          ./"$APPIMAGE" --appimage-extract

          # Bundle FreeType to avoid symbol mismatch with bundled WebKit2GTK
          cp /usr/lib/aarch64-linux-gnu/libfreetype.so.6* squashfs-root/usr/lib/

          # Bundle Fcitx5 client library (required by im-fcitx5.so for daemon communication)
          cp /usr/lib/aarch64-linux-gnu/libFcitx5GClient.so.2* squashfs-root/usr/lib/ 2>/dev/null || true

          # Bundle GTK3 IM modules for Fcitx5/IBus input method support
          IMMODULES_DIR=squashfs-root/usr/lib/aarch64-linux-gnu/gtk-3.0/3.0.0/immodules
          mkdir -p "$IMMODULES_DIR"
          cp /usr/lib/aarch64-linux-gnu/gtk-3.0/3.0.0/immodules/im-fcitx5.so "$IMMODULES_DIR/" 2>/dev/null || true
          cp /usr/lib/aarch64-linux-gnu/gtk-3.0/3.0.0/immodules/im-ibus.so "$IMMODULES_DIR/" 2>/dev/null || true

          # Regenerate immodules.cache from ALL bundled modules (do not overwrite on failure)
          CACHE_FILE=squashfs-root/usr/lib/aarch64-linux-gnu/gtk-3.0/3.0.0/immodules.cache
          gtk-query-immodules-3.0 "$IMMODULES_DIR"/*.so > "${CACHE_FILE}.new" 2>/dev/null && mv "${CACHE_FILE}.new" "$CACHE_FILE" || rm -f "${CACHE_FILE}.new"

          # Download appimagetool and repackage
          wget -q "https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-aarch64.AppImage" -O appimagetool
          chmod +x appimagetool
          ARCH=aarch64 ./appimagetool --no-appstream squashfs-root "$APPIMAGE"

      - name: Collect artifacts
        run: |
          mkdir -p out
          cp target/release/bundle/appimage/*.AppImage out/ember_aarch64.AppImage
          cp target/release/bundle/deb/*.deb out/ember_aarch64.deb
          cp target/release/bundle/rpm/*.rpm out/ember_aarch64.rpm

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-aarch64
          path: out/*

  attach-to-release:
    needs: [build-linux-x64, build-linux-aarch64]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Attach to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/linux-x64/*
            artifacts/linux-aarch64/*
